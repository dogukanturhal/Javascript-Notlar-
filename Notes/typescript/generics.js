/**
 * Türü belirli olmayan veya kendi belirlediğimiz türdeki verileri almamız için kullanırız.
 *
 */
function person(name) {
    return name;
}
var newPerson = person("Doğukan");
console.log(newPerson);
