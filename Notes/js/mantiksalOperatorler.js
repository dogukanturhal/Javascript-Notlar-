/*
    Karar Yapıları
    *****************
    if -> Eğer anlamındadır
    else -> if ile yapılan koşul sağlanmıyorsa yönlendirme yapar
    else if -> if ile kullanılmalıdır eğer if koşulu sağlanmazsa çalışır
    switch case -> if, else if, else yapılarından oluşmuştur ama daha sadedir
    --------------------------------------------------------------------------
    Karşılaştırma Operatörleri
    ****************************
    "==" Eşitse
    "!=" Eşit Değilse
    "<=" Küçük Eşitse
    "<" Küçükse
    ">=" Büyük Eşitse
    ">" Büyükse
    bool değer gönderir yani TRUE veya FALSE
    --------------------------------------------------------------------------
    Mantıksal Operatörler
    ***************************
    "&&" VE koşulu bütün koşulların TRUE olması lazım
    "||" VEYA Operatörü bir koşulun doğru olması lazım
    "!" Değiş Operatörü elde edilen operatörün tersini alır
*/
var arr = [10,50,60,80];
var num1 = 10;
if (num1==10) {
    //Kod Bloğu
} else {
    //Kod Bloğu
}
switch (num1) {
    case 10:
        //Kod Bloğu
        break;

    default:
        // Kod Bloğu
        break;
}
